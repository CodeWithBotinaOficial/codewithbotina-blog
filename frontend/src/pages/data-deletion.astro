---
export const prerender = false;

import { detectLanguage, getRoutePath } from "../lib/i18n";

const language = detectLanguage(
  Astro.cookies,
  Astro.request.headers.get("accept-language"),
);

return Astro.redirect(getRoutePath(language, "dataDeletion"), 302);
---
