---
const apiUrl = import.meta.env.PUBLIC_API_URL || "https://api.codewithbotina.com";
const redirectUrl = `${apiUrl}/api/auth/google`;
const label = "Sign in with Google";
---

<a href={redirectUrl} class="btn-auth">
  {label}
</a>
